setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: production
    RACK_ENV: production
    ENV: production
release:
  image: web
  command:
  - ./release-phase.sh
run:
  web: bundle exec puma -C config/puma.rb